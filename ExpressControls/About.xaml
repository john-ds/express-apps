<ex:ExpressWindow x:Class="ExpressControls.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ex="clr-namespace:ExpressControls"
        mc:Ignorable="d" x:Name="About1"
        Title="About" Width="400" SizeToContent="Height" Style="{DynamicResource ChromeWindowStyle}" d:FontFamily="Inter" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Grid>
        <DockPanel>
            <DockPanel Height="30" Margin="0" VerticalAlignment="Top" Background="{DynamicResource AppColor}" DockPanel.Dock="Top">
                <ex:AppButton x:Name="CloseBtn" IsCloseBtn="True" Background="Transparent" Icon="{DynamicResource CloseWhiteIcon}" NoShadow="True" CornerRadius="0" IconSize="16" Height="30" Padding="7,0" TextVisibility="Collapsed" DockPanel.Dock="Right" ToolTip="{DynamicResource CloseStr}"/>
                <ex:AppButton x:Name="AppLogoBtn" HasNoEffects="True" Focusable="False" Background="Transparent" Icon="{DynamicResource AppWhiteIcon}" NoShadow="True" CornerRadius="0" IconSize="20" Height="30" Padding="6,0,0,0" TextVisibility="Collapsed"/>
                <ex:AppButton x:Name="TitleBtn" HasNoEffects="True" Focusable="False" Text="{Binding Title, ElementName=About1}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Foreground="White" FontSize="12" GapMargin="0" Background="Transparent" NoShadow="True" CornerRadius="0" IconVisibility="Collapsed" Height="30" Padding="7,0"/>
            </DockPanel>
            <Grid>
                <ContentControl x:Name="LogoImg" Focusable="False" Content="{DynamicResource TypeExpressIcon}" Width="70" Margin="30,20,30,0" VerticalAlignment="Top"/>
                <ContentControl Content="{DynamicResource ExpressAppsIcon}" Focusable="False" Width="28" Height="28" Margin="0,110,32,5" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                <ContentControl Content="{DynamicResource JLogo}" Focusable="False" Width="32" Height="32" Margin="34,108,0,2" VerticalAlignment="Top"/>
            </Grid>
            <StackPanel Margin="0,30,30,30">
                <TextBlock x:Name="TitleTxt" TextWrapping="Wrap" FontSize="20" Margin="0,0,0,5" AutomationProperties.HeadingLevel="Level1">
                    <Run x:Name="AppNameTxt" FontWeight="SemiBold">Type Express</Run>
                    <Run x:Name="VersionTxt" FontSize="14">v1.2.3</Run>
                </TextBlock>
                <TextBlock x:Name="LegalTxt" TextWrapping="Wrap" Text="Legal" FontSize="12" Margin="0"/>
                <TextBlock x:Name="DescriptionTxt" TextWrapping="Wrap" FontSize="12" Text="Description" Margin="0,15,0,5"/>
                <ex:AppButton x:Name="WebsiteBtn" Background="{DynamicResource BackSpecialColor}" Click="WebsiteBtn_Click" Icon="{DynamicResource GlobeIcon}" Text="{DynamicResource WebsiteStr}" IconSize="20" Height="32" Margin="0,20,0,0" HorizontalAlignment="Left" HorizontalContentAlignment="Stretch"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</ex:ExpressWindow>
